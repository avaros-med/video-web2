import styled from 'styled-components'
import Colors from '../../../colors'

export const FontWeightBoldStyles = styled.span`
    font-wight: 500;
`

export const TextHintStyles = styled.span`
    color: ${Colors.HINT};
`
